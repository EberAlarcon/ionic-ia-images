import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AI-with-pictures-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
